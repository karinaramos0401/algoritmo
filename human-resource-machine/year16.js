function year16 (num) {
    if(num <0) {
        let negative = num *(-1); 
        console.log (negative);
    } else {
        let positive = num;
        console.log(positive); 
    }
        
}

year16(-10);
year16(2);
